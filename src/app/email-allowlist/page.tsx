import { PagePlaceholder } from "@/components/page-placeholder";

export default function EmailAllowlistPage() {
  return (
    <PagePlaceholder
      title="Email allowlist"
      description="Domains and addresses permitted to land directly in the inbox."
    />
  );
}
